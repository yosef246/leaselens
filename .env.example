# LeaseLens — Environment Variables (TEMPLATE)
# Copy to .env.local and fill real values. .env.local is git-ignored; this file is committed.
# SUPABASE_SERVICE_ROLE_KEY is server-only — never import it into a client component.

# --- Supabase ---
# MUST be the full URL: https://<project-id>.supabase.co — NOT the bare project ref.
# The bare ref fails with "Invalid supabaseUrl" in supabase-js and breaks the Next app.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_XXXXXXXXXXXXXXXXXXXXXXXX
SUPABASE_SERVICE_ROLE_KEY=sb_secret_XXXXXXXXXXXXXXXXXXXXXXXX

# --- Auth: Supabase Auth (Google OAuth + email/password) ---
# No extra keys here. Supabase Auth reuses NEXT_PUBLIC_SUPABASE_URL + ANON_KEY above.
# Google OAuth Client ID/Secret live in the Supabase Dashboard (Auth → Providers → Google),
# NOT in this file. (Clerk was evaluated and dropped — Supabase Auth is the chosen stack.)

# --- Anthropic (Claude sonnet-5) ---
ANTHROPIC_API_KEY=sk-ant-XXXXXXXXXXXXXXXXXXXXXXXX

# --- OpenAI (embeddings only) ---
OPENAI_API_KEY=sk-XXXXXXXXXXXXXXXXXXXXXXXX

# --- PostHog (analytics, P7) ---
NEXT_PUBLIC_POSTHOG_KEY=phc_XXXXXXXXXXXXXXXXXXXXXXXX
NEXT_PUBLIC_POSTHOG_HOST=https://us.posthog.com

# --- Feature flags ---
# Live Knesset scraping stays OFF (data/laws/*.txt is source of truth, per DECISIONS.md D1)
FEATURE_LAW_SCRAPING_ENABLED=false
