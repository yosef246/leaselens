# LeaseLens — Locked Owner Decisions

These are authoritative decisions made by the project owner. Every agent MUST read this
file and comply. These override any conflicting detail in MASTER_PROMPT.md or REQUIREMENTS.md.
Do NOT re-open or re-decide these without explicit owner approval.

---

## D1 — Law corpus source & P1 starter set

- **`data/laws/*.txt` are the single source of truth.** Live Knesset scraping is NOT part of P1.
- Live scraping stays a future nice-to-have, behind a **feature flag**, and is **NOT** in any
  P1 completion criterion.
- The owner will hand-prepare the corpus files. **Do NOT mark P1 done until the actual .txt
  files exist in the repo.** Do NOT proceed to P2 without them.

**P1 starter set (this list supersedes the "Target Laws" list in MASTER_PROMPT.md):**

| File | Law |
|---|---|
| `data/laws/rental-fair.txt` | חוק השכירות ההוגנת, התשע"ז-2017 (פרקים א'–ג', סעיפים 25 ואילך הרלוונטיים) |
| `data/laws/tenant-protection.txt` | חוק הגנת הדייר [נוסח משולב], התשל"ב-1972 (סעיפים על תניות פסולות) |
| `data/laws/contracts-general.txt` | חוק החוזים (חלק כללי), התשל"ג-1973 (סעיפים 12, 30, 39) |
| `data/laws/contracts-remedies.txt` | חוק החוזים (תרופות בשל הפרת חוזה), התשל"א-1970 |

**File format (parser MUST handle exactly this pattern):**
```
<כותרת החוק>
<מספר סעיף> <טקסט הסעיף>
<מספר סעיף> <טקסט הסעיף>
...
```
i.e. each file = a title line, then repeating `section-number → section-text` blocks.
The Hebrew-law chunker must parse this pattern (title → section number → text).

---

## D2 — Legal disclaimer (mandatory, verbatim)

A disclaimer is REQUIRED at every touchpoint that produces a legal-analysis result — not just
one "bonus" page.

**Mandatory placements:**
1. **Onboarding** — first screen on entry, with an "הבנתי" checkbox (must be acknowledged).
2. **Persistent footer** — fixed on every contract-analysis result screen.
3. **Pre-export/share modal** — a small modal shown before exporting or sharing a result.

**Exact wording — use WORD FOR WORD. Do NOT alter without owner approval:**

> ⚠️ מידע כללי בלבד — אינו ייעוץ משפטי. האפליקציה מסתמכת על ניתוח אוטומטי מול טקסטים חוקיים ועשויה לטעות או להחמיץ הקשר. להחלטה משפטית ממשית פנה/י לעורך/ת דין מוסמך/ת.

---

## D3 — QA acceptance fixtures (synthetic, generated by qa-tester)

Fixtures are synthetic — no real-world contracts needed. Create under `tests/fixtures/contracts/`:

**`contract-illegal-01.pdf`** — one-page Hebrew rental contract containing 3 known illegal clauses,
each documented in the test metadata:
1. Clause waiving tenant protection in advance — illegal per חוק הגנת הדייר.
2. Clause allowing landlord access without notice — violates חוק השכירות ההוגנת.
3. Immediate-eviction-without-process clause — violates חוק החוזים.

**`contract-clean-02.pdf`** — a clean, compliant contract with NO illegal clauses
(for false-positive testing).

Each fixture has a parallel `.json` with `expected_findings` (the list of clauses the system MUST
detect + the relevant law). This JSON is the **ground truth** for P1 acceptance testing.

---

## D4 — Centralized DB access layer (REQUIRED before P5 / backend)

Approved the service-role + application-layer `user_id` filtering model — WITH this hardening,
which makes "forgot the filter" impossible:

- **All Supabase access goes through `src/lib/db/*.ts` only.** No `supabase.from(...)` in API
  routes or server actions directly.
- **Every function in that layer takes `userId: string` as a mandatory, non-nullable FIRST
  parameter.** The db-layer function injects `.eq('user_id', userId)` — never the caller.
- **A lint rule (ESLint, or a simple CI grep) blocks `import { supabase }` (the client) in any
  file outside `src/lib/db/`.** Enforced in CI.
- **RLS `deny-all` stays enabled** as a fail-closed safety net.

This layer is a precondition for starting backend work (P5).

> **Directory convention:** the owner's paths (`src/lib/db/`, `src/lib/laws/mapping.ts`) imply a
> `src/` root. The project standardizes on the Next.js `src/` layout: `src/app/`, `src/lib/`,
> `src/components/`, with `scripts/`, `data/`, `tests/`, `docs/`, `supabase/` at the repo root.
> (Confirm at P0 bootstrap; this is the modern Next.js default and matches D4/D6 paths.)

---

## D5 — Print disclaimer must be in @media print

Approved the small print button on `/contract/[id]` hosting the D2 modal before `window.print()`.
Requirement: the disclaimer (D2 wording, verbatim) must live in the **print stylesheet
(`@media print`)**, not only on screen — it MUST appear in the PDF produced by printing.

---

## D6 — Law category/is_binding mapping lives in ONE file

Approved deriving `category` / `is_binding` per source file. Constraint: the mapping lives in
**`src/lib/laws/mapping.ts`** (or a similar single config file) ONLY — not scattered across the
parser, services, or tests. Everyone imports the category from that one place. Eases adding a 5th
law or flipping a law to non-binding later.

---

## D7 — P7 performance & failure requirements

Approved as a P7 note (not blocking now). Two hard requirements for P7:

1. **Real p95 measurement** of analysis time on a ~20-page contract — measured, not theoretical.
2. **Clear mid-chunk failure fallback** — if a chunk fails partway through analysis, the user must
   NOT be left with a silent partial analysis; show an explicit notice/error state.

---

## D1 — AMENDED (corpus expanded 4 → 11 laws, manifest-driven)

The owner rebuilt and expanded the P1 corpus. **This amendment supersedes the D1 table above.**

- **Source of truth:** `data/laws/*.txt` (11 files) + **`data/laws/manifest.json`** (note: manifest lives
  inside `data/laws/`, not `data/`). `rental-fair.txt` (חוק שכירות הוגנת) was REMOVED; the corpus now
  uses `rental-law-fixed.txt` (חוק השכירות והשאילה, התשל״א-1971).
- **The 11 laws** (see manifest for full metadata — `title`, `short_title`, `year`, `year_hebrew`,
  `category`, `priority`): 01-contracts-general, 02-contracts-remedies, 03-sale, 04-bailees, 05-land,
  06-standard-contracts, 07-tenant-protection, 08-unjust-enrichment, 09-guarantee, 10-limitations,
  rental-law-fixed.
- **`embed-laws` MUST iterate the manifest, NOT a directory glob.** Only manifest-listed files are
  embedded; every chunk's metadata (law title/short_title/year/category/priority + is_binding) comes
  from the manifest. A `.txt` on disk that isn't in the manifest is ignored (and should not exist).
- **File format:** title line (`שם החוק, שנה עברית–שנה`) → optional `פרק X: ...` / `סימן X: ...`
  headers → section `^\d+[א-ת]?\. <כותרת> [תיקון: ...]` → sub-clauses `(א)`, `(ב)`, ... each on its
  own line followed by text.
- **Chunking:** by section (regex `^\d+[א-ת]?\.`). Fold a section's sub-clauses into its chunk text.
  Carry the enclosing `פרק` (chapter) and `סימן` (sign) as chunk **metadata** (context), plus any
  `[תיקון: ...]` amendment marker. `is_binding = true` for all 11 (enacted statutes); `category` from
  the manifest (D6 mapping reads the manifest).
- **D3 fixture remap (for QA):** the "שכירות הוגנת" citation maps to the fair-rental provisions that
  live as sections **~25א–25כד inside `rental-law-fixed.txt`** (חוק השכירות והשאילה) — that 2017
  amendment was inserted into the שכירות והשאילה law. Apply this remap at the QA stage.
